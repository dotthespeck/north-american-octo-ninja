<div class="questions">
<a href="/questions/new">Ask a new question</a>
</div>

<div>
<% @questions.each do |question| %>
<a href="/questions/<%=question.id%>"><h2><%= question.title %> from <%= question.user.name %></h2></a>
  <h4>Question: <%= question.description %></h4>
  <div class="button">
  <a href="/questions/<%=question.id%>/answers/new">Submit an answer</a>
  </div>
</div>

<div>
  <% question.answers.each do |answer| %>
    <h4>Answer: <%= answer.answer %> from <%= answer.user.name %></h4>
    <% end %>
    <% end %>
</div>
